{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uhdi/expressions.schema.json",
  "$defs": {
    "ExprRef": {
      "description": "Key into the expressions pool. Opaque: unique within the pool, no structure a consumer may rely on.",
      "type": "string"
    },
    "VarRef": {
      "description": "Key into the variables pool. Opaque: unique within the pool, no structure a consumer may rely on.",
      "type": "string"
    },
    "FieldName": {
      "description": "Name of the aggregate field this operand fills. Emitted on the operands of `'{` so a consumer joins them to `types.*.members` by name rather than by position.",
      "type": "string"
    },
    "LeafVar": {
      "type": "object",
      "required": [
        "varRef"
      ],
      "additionalProperties": false,
      "properties": {
        "varRef": {
          "$ref": "#/$defs/VarRef"
        },
        "name": {
          "$ref": "#/$defs/FieldName"
        }
      }
    },
    "LeafConst": {
      "type": "object",
      "required": [
        "constant"
      ],
      "additionalProperties": false,
      "properties": {
        "constant": {
          "type": "integer"
        },
        "width": {
          "type": "integer",
          "minimum": 0
        },
        "name": {
          "$ref": "#/$defs/FieldName"
        }
      }
    },
    "LeafBitVec": {
      "type": "object",
      "required": [
        "bitVector"
      ],
      "additionalProperties": false,
      "properties": {
        "bitVector": {
          "type": "string",
          "pattern": "^[01xzXZ?]+$"
        },
        "name": {
          "$ref": "#/$defs/FieldName"
        }
      }
    },
    "LeafSig": {
      "type": "object",
      "required": [
        "sigName"
      ],
      "additionalProperties": false,
      "properties": {
        "sigName": {
          "type": "string"
        },
        "name": {
          "$ref": "#/$defs/FieldName"
        }
      }
    },
    "LeafRef": {
      "type": "object",
      "required": [
        "exprRef"
      ],
      "additionalProperties": false,
      "properties": {
        "exprRef": {
          "$ref": "#/$defs/ExprRef"
        },
        "name": {
          "$ref": "#/$defs/FieldName"
        }
      }
    },
    "OpNode": {
      "type": "object",
      "required": [
        "opcode",
        "operands"
      ],
      "additionalProperties": false,
      "properties": {
        "opcode": {
          "enum": [
            "+",
            "-",
            "*",
            "/",
            "%",
            "neg",
            "&",
            "|",
            "^",
            "~",
            "&&",
            "||",
            "!",
            "andr",
            "orr",
            "xorr",
            "<<",
            ">>",
            ">>>",
            "==",
            "!=",
            "<",
            ">",
            "<=",
            ">=",
            "===",
            "!==",
            "==?",
            "!=?",
            "?:",
            "{}",
            "R{}",
            "[]",
            "pad",
            "asUInt",
            "asSInt",
            "cvt",
            "asClock",
            "asAsyncReset",
            "'{",
            ".",
            "idx",
            "Mux",
            "Cat",
            "Fill",
            "VecInit"
          ]
        },
        "operands": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Operand"
          }
        },
        "fieldName": {
          "type": "string"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "opcode": { "const": "." } },
            "required": ["opcode"]
          },
          "then": {},
          "else": { "not": { "required": ["fieldName"] } }
        }
      ]
    },
    "Operand": {
      "oneOf": [
        {
          "$ref": "#/$defs/LeafVar"
        },
        {
          "$ref": "#/$defs/LeafConst"
        },
        {
          "$ref": "#/$defs/LeafBitVec"
        },
        {
          "$ref": "#/$defs/LeafSig"
        },
        {
          "$ref": "#/$defs/LeafRef"
        },
        {
          "$ref": "#/$defs/OpNode"
        }
      ]
    },
    "Expression": {
      "$ref": "#/$defs/OpNode"
    }
  },
  "type": "object",
  "additionalProperties": {
    "$ref": "#/$defs/Expression"
  }
}